[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hh-resume-raiser"
version = "0.1.0"
description = "Local automation for raising a personal resume on HH.ru"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.62,<2",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.12,<1",
]

[project.scripts]
hh-resume-raiser = "hh_raiser.cli:main"

[tool.setuptools.packages.find]
include = ["hh_raiser*"]

[tool.ruff]
line-length = 100
target-version = "py312"
